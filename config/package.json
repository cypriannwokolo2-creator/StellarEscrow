{
  "name": "@stellar-escrow/config",
  "version": "1.0.0",
  "description": "Centralized configuration service for StellarEscrow",
  "scripts": {
    "validate": "ts-node validate.ts",
    "validate:dev": "ts-node validate.ts --env development",
    "validate:staging": "ts-node validate.ts --env staging",
    "validate:prod": "ts-node validate.ts --env production"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5",
    "ajv": "^8.12.0",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.2"
  }
}
